import type { NextPage } from 'next';
import { Layout } from '../templates/global';

const Home: NextPage = () => {
  return (
    <Layout>
      <>Hello!</>
    </Layout>
  );
};

export default Home;
