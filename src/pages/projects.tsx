import type { NextPage } from 'next';
import { Layout } from '../templates/global';

const Home: NextPage = () => {
  return (
    <Layout>
      <>Projects!</>
    </Layout>
  );
};

export default Home;
